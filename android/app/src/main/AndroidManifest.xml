<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cleverraven.cataclysmdda"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">
    <!-- OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" /> <!-- Allow writing to external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- Allow access to the vibrator -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher_1"
        android:isGame="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <provider
            android:authorities="com.cleverraven.cataclysmdda.fileprovider"
            android:name="androidx.core.content.FileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/fpath"/>
        </provider>
        <activity android:name=".Helper" android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity
            android:name=".ModList"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".WelcomScreen"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".NoDispaly"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
         Example of setting SDL hints from AndroidManifest.xml:
        <meta-data android:name="SDL_ENV.SDL_ACCELEROMETER_AS_JOYSTICK" android:value="0"/>
        -->
        <activity
            android:name=".SplashScreen"
            android:configChanges="keyboardHidden|keyboard|orientation|screenSize|navigation"
            android:keepScreenOn="true"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:screenOrientation="userLandscape">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".CataclysmDDA"
            android:configChanges="keyboardHidden|keyboard|orientation|screenSize|navigation"
            android:keepScreenOn="true"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:screenOrientation="userLandscape" />
    </application>

</manifest>
